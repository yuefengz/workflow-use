import { Edge } from '@xyflow/react';

// Define any custom edge types here
export const edgeTypes = {
  // For example:
  // customEdge: CustomEdgeComponent,
};

// Initial edges can be empty since we're loading them from the backend
export const initialEdges: Edge[] = [];
