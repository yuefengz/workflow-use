import { Node } from '@xyflow/react';

// Define any custom node types here
export const nodeTypes = {
  // For example:
  // customNode: CustomNodeComponent,
};

// Initial nodes can be empty since we're loading them from the backend
export const initialNodes: Node[] = [];
